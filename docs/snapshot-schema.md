---
title: "Desktop Bridge Snapshot Schema"
doc_id: "doc-schema-desktop-bridge-snapshot"
doc_type: "schema"
status: "active"
canonical: true
created_at: "2026-05-02T00:00:00Z"
updated_at: "2026-05-02T00:00:00Z"
revision_count: 1
last_updated_by_agent: "codex"
last_updated_by_session: "codex-desktop-passive-observer-mvp"
tags:
  - evaos
  - desktop-bridge
  - schema
  - codex
---

# Desktop Bridge Snapshot Schema

## Summary

This document defines the MVP JSON contract for `evaos-desktop-bridge`. The schema is intentionally small, redacted by default, and limited to visible desktop observation commands.

## Current Status

Active for MVP issue `100yenadmin/evaos-desktop-bridge#1`.

## Last Update

- Updated At: 2026-05-02T00:00:00Z
- Agent: codex
- Session: codex-desktop-passive-observer-mvp
- Revision Count: 1

## Change Log

- 2026-05-03T00:00:00Z - Added capabilities, latest, audit-tail, frontmost, windows, and inspect command data notes.
- 2026-05-02T00:00:00Z - Initial MVP schema for status, focus, snapshot, and AX tree commands.

## Envelope

Every successful or failed command returns one JSON object:

```json
{
  "schema_version": "2026-05-02.mvp1",
  "command": "codex.snapshot",
  "target": "codex",
  "timestamp": "2026-05-02T00:00:00Z",
  "ok": true,
  "data": {},
  "warnings": [],
  "errors": [],
  "audit_id": "audit-0123456789abcdef"
}
```

Fields:

- `schema_version`: Stable MVP schema id.
- `command`: One of the bridge allowlisted command ids, such as `status`, `capabilities`, `latest`, `audit_tail`, `codex.snapshot`, or `codex.ax_tree`.
- `target`: `desktop` or `codex`.
- `timestamp`: UTC ISO-8601 timestamp generated by the bridge.
- `ok`: Boolean command outcome.
- `data`: Command-specific payload.
- `warnings`: Non-fatal issues, already redacted.
- `errors`: Structured fatal issues, already redacted.
- `audit_id`: Identifier matching the local append-only audit log record.

## Structured Errors

Errors use this shape:

```json
{
  "code": "permission_missing",
  "message": "Accessibility permission is required.",
  "guidance": "Open System Settings > Privacy & Security > Accessibility ...",
  "permission": "accessibility"
}
```

`permission` is present only when the error maps to a macOS permission.

## Command Data

`status` returns:

```json
{
  "platform": "Darwin",
  "app": {
    "name": "Codex Desktop",
    "process_name": "Codex",
    "installed": true,
    "running": true,
    "pid": 12345,
    "paths": ["~/Applications/Codex.app"]
  },
  "permissions": {
    "accessibility": {"status": "granted", "guidance": "..."},
    "screen_recording": {"status": "unknown", "guidance": "..."}
  },
  "safety": {
    "read_only": true,
    "sends_prompts": false,
    "uses_internal_mutation_rpc": false,
    "reads_session_databases": false
  }
}
```

`capabilities` returns:

```json
{
  "modes": {
    "eyes": "Read-only visible desktop observation with redaction and caps.",
    "hands": "Visible focus only in this CLI; no typing, clicking, prompt sending, or session mutation.",
    "brain": "Local Eva/OpenClaw announcement queue contract with external relay left to future sinks."
  },
  "commands": [
    {"id": "codex.snapshot", "target": "codex", "mode": "read_only"}
  ],
  "forbidden": ["send_prompts_or_messages"],
  "data_minimization": {
    "redacts_home_paths": true,
    "caps_ax_nodes": true,
    "append_only_audit_log": true
  }
}
```

`latest` returns:

```json
{
  "latest": {
    "schema_version": "2026-05-02.mvp1",
    "command": "codex.inspect",
    "target": "codex",
    "ok": true,
    "data": {}
  }
}
```

`audit_tail` returns:

```json
{
  "records": [
    {
      "audit_id": "audit-0123456789abcdef",
      "command": "codex.snapshot",
      "target": "codex",
      "ok": true
    }
  ],
  "count": 1,
  "limit": 20
}
```

`codex.frontmost` returns:

```json
{
  "frontmost_app": "Codex",
  "window_title": "Codex",
  "codex_frontmost": true
}
```

`codex.windows` returns:

```json
{
  "windows": [
    {
      "index": 0,
      "title": "Codex",
      "role": "AXWindow",
      "bounds": {"x": 0, "y": 0, "width": 1200, "height": 800},
      "codex_frontmost": true
    }
  ],
  "count": 1
}
```

`codex.threads` returns:

```json
{
  "threads": [
    {
      "visible_id": "visible-0-...",
      "index": 0,
      "title": "Thread title",
      "role": "AXStaticText",
      "bounds": {"x": 0, "y": 0, "width": 200, "height": 40},
      "center": {"x": 100, "y": 20},
      "confidence": "medium",
      "source": "ax"
    }
  ],
  "count": 1,
  "max_items": 50,
  "source": "ax"
}
```

`codex.focus` returns:

```json
{
  "focused": true
}
```

With `--dry-run`, it returns:

```json
{
  "would_focus": true,
  "focused": false
}
```

`codex.select_thread` returns:

```json
{
  "selected": false,
  "would_select": true,
  "thread_id": "visible-0-...",
  "target": {"visible_id": "visible-0-...", "title": "Thread title"}
}
```

`codex.snapshot` returns:

```json
{
  "timestamp": "2026-05-02T00:00:00Z",
  "frontmost_app": "Codex",
  "window_title": "Codex",
  "screenshot_path": "~/Library/Application Support/evaos-desktop-bridge/screenshots/20260502T000000Z.png",
  "max_chars": 4000
}
```

`codex.inspect` returns a compact page map:

```json
{
  "frontmost": {"codex_frontmost": true},
  "windows": [{"index": 0, "title": "Codex"}],
  "ax": {"nodes": [{"role": "AXButton", "name": "New chat"}]},
  "summary": {
    "window_count": 1,
    "codex_frontmost": true,
    "visible_buttons": [{"role": "AXButton", "name": "New chat"}],
    "visible_text": []
  }
}
```

`codex.ax_tree` returns:

```json
{
  "nodes": [
    {"role": "AXWindow", "name": "Codex"},
    {"role": "AXButton", "name": "New Chat"}
  ],
  "truncated": false,
  "max_nodes": 200
}
```

`codex.app_server.status` returns read-only app-server availability, selected CLI
path/version, handshake health, transport, and method allowlists.
`codex.app_server.remote_control_status` returns a read-only Codex native
remote-control and Connections readiness probe. `codex.app_server.threads`
returns capped thread summaries from allowed app-server read methods only; a
reachable app-server with no returned threads reports `thread_state: idle`.

Guarded actions such as `codex.continue_thread`,
`customer_mac.iphone_mirroring_swipe_left`, and
`customer_mac.iphone_mirroring_send_approved_message` use the same envelope
shape. Live use requires a matching dry-run audit id.

`queue.append` returns a local queue event, and `queue.list` returns capped queue events for Eva/OpenClaw announcement routing.

## Redaction and Caps

- Current user home paths are converted to `~`.
- Generic `/Users/<name>` paths are converted to `~`.
- Secret-like API keys, bearer tokens, and authorization headers are replaced with `<redacted-secret>`.
- Snapshot text is capped by `--max-chars`.
- AX tree output is capped by `--max-nodes` and includes roles/names only.

## Non-Goals

This schema does not include prompt sending, session database rows, auth files, token material, or mutation-capable app-server calls.
