name: codex_ax_tree
version: "1.0"
description: Run evaos-desktop-bridge codex ax-tree against the visible Codex Desktop workspace.
tags: [codex, desktop, visible, read-only]
parameters: {}
preconditions:
  - file_exists: /Users/lume/.config/superpowers/worktrees/evaos-desktop-bridge/feat-codex-desktop-passive-observer/.venv/bin/python
  - process_running: Codex
steps:
  - id: run_bridge
    backend: native_api
    action: run_command
    params:
      command:
        - /Users/lume/.config/superpowers/worktrees/evaos-desktop-bridge/feat-codex-desktop-passive-observer/.venv/bin/python
        - -m
        - evaos_desktop_bridge.cli
        - codex
        - ax-tree
        - --json
      capture_stdout: true
    timeout_ms: 30000
    on_failure: fail
postconditions:
  - always: true
agent_hints:
  danger_level: safe
  side_effects: []
  reversible: true
